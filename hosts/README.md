# Host-specific configuration

Each optional directory below `hosts/` is named after `hostnamectl --static`
and mirrors paths below the home directory. Use this layer for display layouts,
hardware-specific KDE rules, and other configuration that should not be shared
by every machine.

The `dots link` command automatically includes the current host package when it
exists.

